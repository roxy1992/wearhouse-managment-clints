import React from 'react';

const Blogs = () => {
    return (
        <div>
            <h2 className='text-center'>Difference between javascript and nodejs</h2>
            <p className='text-center'>JavaScript is a common programming language that runs the JavaScript engine in any browser. Where Node JS is an interpreter or running environment for a JavaScript programming language that contains many exaggerations, it requires a library that can be easily accessed from JavaScript programming for better use.</p>
            <h2 className='text-center'>When should you use nodejs and when should you use mongodb</h2>
            <p className='text-center'>There are many web servers built with Nodez that will use MongoDB to store data.

                MongoDB offers an API library that runs in a Nodejs application so you can give programmatic access to MongoDB so you can create databases and then add, query, update or delete data from the MongoDB database. MongoDB also has API libraries for other programming environments like Python, Java etc.</p>
            <h2 className='text-center'>Differences between sql and nosql databases.</h2>
            <p className='text-center'>The biggest decision when choosing a database is to choose a relational (SQL) or non-relational (NoSQL) data structure. Although both databases are effective alternatives, there are some key differences between the two that users must keep in mind when making decisions.</p>
            <h2 className='text-center'>What is the purpose of jwt and how does it work</h2>
            <p className='text-center'>For beginner developers, JSON is a JavaScript object notation and is a text-based format for transmitting data across web applications. It stores information in an easily accessible way for both developers and computers. It can be used as a data format by any programming language and is quickly becoming the preferred syntax for APIs beyond XML.</p>
        </div>
    );
};

export default Blogs;